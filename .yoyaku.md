# WebUI 后端接口约定（策略内容 / 状态 / 统计 / 配置文件 / WebRender）

本目录的接口为 WebUI 前端提供机器人运行时数据与配置管理能力：

| 实现文件 | 覆盖范围 |
| --- | --- |
| `policy.py` | `data/url_audit` 与 `data/filter_words` 两个可写目录 |
| `api.py` | 状态、统计、配置文件等既有 `/api/*` 接口 |
| `webrender.py` | WebRender 的状态查询、启停控制与渲染测试 |

本文是前后端共同遵守的接口约定，改动接口时请同步更新本文与
前端仓库 [akari-bot-webui](https://github.com/Teahouse-Studios/akari-bot-webui)
的 `src/mock/api.js`。

跨进程取数的约定：状态、统计来源与 WebRender 均由**服务端进程**持有。
`bots/web` 是客户端进程，相关字段一律经 JobQueue RPC 取自服务端；
服务端离线时接口只降级对应字段，不影响其余数据。

## 1. 通用约定

- **鉴权**：所有接口都需要 `Authorization: Bearer <JWT>`，与既有 `/api/*` 一致；
  未通过时返回 401。
- **命名**：URL 审计名单的路径参数取值固定为 `allowlist` / `blocklist`，
  过滤词库的路径参数为分类名（即 `data/filter_words/<分类名>.txt` 的文件主干）。
- **只读与可写**：随主仓分发的 `global.txt` 永远只读，接口只写同目录下的
  `user.txt`，即 `data/url_audit/<名单>/user.txt` 与 `data/filter_words/*.txt`。
- **即时生效**：URL 名单由核心按文件元信息判定缓存，写入后立刻生效；
  过滤词库写入后由接口主动触发核心进程重载（RPC `server.reload_filter_words`），
  返回体里的 `runtime_synced` 表示核心是否重载成功，为 `false` 时应提示用户重启机器人。
- **写接口返回 `changed`**：删除或追加一个已存在的对象时不报错，而是返回
  `200` 且 `changed: false`，方便前端做幂等操作（例如重复点击删除）。

### 1.1 版本号（revision）约定

每个可写资源都有版本号，用于「增量同步」与「乐观并发控制」：

- 版本号是 16 位十六进制字符串，由**文件元信息**（文件名、`mtime_ns`、大小）哈希得到；
  文件不存在时为 `missing`。
- 读取响应始终携带 `revision`：
  - `/api/url-audit` → 两份名单的合并版本号；
  - `/api/url-audit/{list}` → 该名单（含 `global.txt` 与 `user.txt`）的版本号；
  - `/api/filter-words` → 词库目录（全部 `*.txt` 的文件集合与元信息）的版本号；
  - 单个分类的版本号见 `category.revision`。
- 读取时可用 `?revision=<上次读到的值>` 做条件请求，未变化时返回 **304** 空响应。
- 写入时可在请求体或查询参数里带上 `revision`：与当前不一致时返回 **409**
  `revision_mismatch`，并在响应头 `X-Policy-Revision` 给出当前版本号；
  调用方重新拉取后再提交即可。不传 `revision` 表示不做并发校验。
- 轮询建议：优先调用轻量的 `GET /api/policy/revision`（只做文件 stat），
  比较 `revision` 变化后再拉取具体内容。

### 1.2 错误码

下表为策略内容接口（第 2、3 章）的原因码；状态、统计、配置文件与 WebRender 接口的
原因码见各自章节。

| 状态码 | `detail` | 含义 |
| --- | --- | --- |
| 400 | `invalid_json` / `invalid_body` | 请求体不是 JSON 对象，或字段类型不符 |
| 401 | - | 未通过 JWT 校验 |
| 404 | `unknown_list` | 名单名不是 `allowlist` / `blocklist` |
| 404 | `category_not_found` | 词库分类文件不存在 |
| 409 | `revision_mismatch` | 版本号已过期，需重新拉取 |
| 422 | URL 规则原因码 | `invalid_url`、`invalid_regex`、`unsafe_regex`、`broad_regex`、`too_many_rules`、`too_many_regex`、`file_too_large` |
| 422 | 词库原因码 | `invalid_category`、`word_too_long`、`too_many_words`、`too_many_categories`、`file_too_large` |

错误码是稳定的机器可读标识，前端应自行本地化展示，不要依赖后端文案。

## 2. URL 审计名单

对应目录：`data/url_audit/allowlist/user.txt`、`data/url_audit/blocklist/user.txt`。
规则写法与 `url-audit` 命令一致：每行一条精确 URL，正则规则以 `regex:` 开头。

### 2.1 读取

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/url-audit` | 两份名单全量（`allowlist` + `blocklist`） |
| `GET` | `/api/url-audit/{list}` | 单份名单 |
| `GET` | `/api/url-audit/query?url=<URL>` | 规则试算：某个 URL 是否被放行/拦截 |

`GET /api/url-audit/allowlist?revision=abc123` 响应示例：

```json
{
  "name": "allowlist",
  "revision": "0f1e2d3c4b5a6978",
  "files": [
    {
      "source": "global", "writable": false, "exists": true,
      "path": "./data/url_audit/allowlist/global.txt",
      "revision": "9a8b7c6d5e4f3210", "size": 128, "updated_at": 1735689600.0,
      "rule_count": 1, "invalid_lines": 0, "ignored": false
    },
    {
      "source": "user", "writable": true, "exists": true,
      "path": "./data/url_audit/allowlist/user.txt",
      "revision": "1234567890abcdef", "size": 64, "updated_at": 1735689700.0,
      "rule_count": 2, "invalid_lines": 0, "ignored": false
    }
  ],
  "rules": [
    {"value": "https://repo.example.test/release", "serialized": "https://repo.example.test/release", "regex": false, "source": "global"},
    {"value": "https://example.test/docs/1", "serialized": "https://example.test/docs/1", "regex": false, "source": "user"},
    {"value": "https://[a-z]+\\.example\\.test/", "serialized": "regex:https://[a-z]+\\.example\\.test/", "regex": true, "source": "user"}
  ],
  "user_rules": ["https://example.test/docs/1", "regex:https://[a-z]+\\.example\\.test/"],
  "limits": {"max_rules": 256, "max_regex_rules": 64, "max_rule_length": 500, "max_url_length": 4096, "max_file_bytes": 65536}
}
```

要点：

- `rules` 是**实际生效**的规则（`global.txt` 与 `user.txt` 合并、去重后的顺序，
  `source` 区分来源）；`user_rules` 是用户文件里的原始顺序，供整体替换时原样回传。
- `invalid_lines` 用于提示手工编辑留下的坏行；`ignored` 表示文件超限而被核心忽略。
- 接口写入不会保留 `user.txt` 中的注释与空行，只保留规则本身。

`GET /api/url-audit/query?url=https://example.test/docs/1` 响应示例：

```json
{
  "url": "https://example.test/docs/1",
  "normalized": "https://example.test/docs/1",
  "valid": true,
  "allowed": true,
  "blocked": false,
  "matches": {"allowlist": [{"value": "https://example.test/docs/1", "serialized": "https://example.test/docs/1", "regex": false, "source": "user"}], "blocklist": []}
}
```

URL 非法时返回 `valid: false` 与 `reason`（如 `invalid_url`），其余字段保持默认值。
判定语义与运行时一致：阻止列表优先，同时命中两份名单时 `blocked` 为 `true`、`allowed` 为 `false`。

### 2.2 写入

| 方法 | 路径 | 请求体 / 参数 |
| --- | --- | --- |
| `POST` | `/api/url-audit/{list}/rules` | `{"value": "https://example.test/", "regex": false, "revision": "…"}` |
| `PUT` | `/api/url-audit/{list}/rules` | `{"rules": ["https://a.test/", "regex:https://[a-z]+\\.test/"], "revision": "…"}` |
| `DELETE` | `/api/url-audit/{list}/rules` | 查询参数 `value`、`regex`（`true` 表示正则）、`revision` |

- `POST` 追加一条规则；`value` 传规则本身，正则**不要**带 `regex:` 前缀，改用 `regex: true`。
- `PUT` 整体替换用户规则文件，逐条校验并去重；`rules` 为空列表即清空用户规则。
  推荐前端直接回传上一次读取到的 `user_rules`，做到幂等的全量同步。
- `DELETE` 只删除用户规则，主仓 `global.txt` 中的规则无法通过接口删除（`changed: false`）。
- 三个写接口都返回该名单的最新完整结构（与 `GET /api/url-audit/{list}` 相同），
  额外多一个 `changed` 字段，前端可据此直接刷新本地状态。

## 3. 过滤词库

对应目录：`data/filter_words/*.txt`。文件名即分类名（`[0-9A-Za-z_-]{1,64}`，
不得超过 64 个文件），命中后机器人把关键词替换为 `[吃掉了:local_<分类名>]`。

### 3.1 读取

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/filter-words` | 全部分类及词条 |
| `GET` | `/api/filter-words/{category}` | 单个分类 |

`GET /api/filter-words` 响应示例：

```json
{
  "revision": "abcdef0123456789",
  "directory": "./data/filter_words",
  "categories": [
    {
      "name": "politics", "label": "local_politics", "file": "politics.txt",
      "count": 2, "words": ["示例词A", "示例词B"],
      "exists": true, "revision": "0123456789abcdef", "size": 18, "updated_at": 1735689700.0
    }
  ],
  "limits": {"max_categories": 64, "max_words_per_category": 20000, "max_word_length": 128, "max_file_bytes": 1048576}
}
```

单分类接口返回同样的 `category` 对象，外层为 `{"revision": "<目录版本号>", "category": {…}}`。

### 3.2 写入

| 方法 | 路径 | 请求体 / 参数 |
| --- | --- | --- |
| `PUT` | `/api/filter-words/{category}` | `{"words": ["词A", "词B"], "revision": "…"}` |
| `DELETE` | `/api/filter-words/{category}` | 查询参数 `revision` |
| `POST` | `/api/filter-words/{category}/words` | `{"words": ["词C"], "revision": "…"}` |
| `DELETE` | `/api/filter-words/{category}/words` | 查询参数 `word`（可重复）、`revision` |

- `PUT` 整体替换分类词条：自动去空白、丢弃空行、按首次出现顺序去重；
  分类不存在时创建；`words` 为空列表即删除该分类文件。
- `POST` 追加词条（分类不存在时创建），与既有词条合并去重，空数组返回 400。
- `DELETE …/words?word=示例词A&word=示例词B` 删除指定词条，返回 `removed` 数量。
- 写入响应形如：

```json
{
  "changed": true,
  "revision": "abcdef0123456789",
  "category": {"name": "politics", "label": "local_politics", "file": "politics.txt", "count": 2, "words": ["示例词A", "示例词B"], "exists": true, "revision": "0123456789abcdef", "size": 18, "updated_at": 1735689700.0},
  "runtime_synced": true
}
```

分类被删除时 `category` 为 `null`、`changed` 为 `true`；对不存在的分类执行删除时
`changed` 为 `false`，接口不返回 404。

## 4. 状态接口（`/api/server-info`）

`GET /api/server-info`（限流 6/分钟）在既有的 `os` / `cpu` / `memory` / `disk` 之外新增：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `bot.jobqueue_backend` | `string \| null` | 当前 JobQueue 后端类型：`database` 或 `websocket`；服务端不可用时为 `null` |
| `bot.command_parsed` | `int \| null` | 自服务端启动以来已处理的命令数（与 `~status` 命令同源） |
| `bot.message_parsed` | `int \| null` | 自服务端启动以来已处理的消息数 |
| `processes.items[]` | `array` | 各进程内存占用，见下表 |
| `processes.failures[]` | `array` | 未能取到指标的进程（`name` + `reason`） |
| `processes.error` | `string \| null` | 整批采集失败时的原因；正常为 `null` |

`processes.items[]` 的每项：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | `string` | 进程名（`Server`、平台名、`daemon`、`jobqueue-hub`） |
| `pid` | `int \| null` | 进程 PID；非 Peer 进程可能缺失 |
| `memory` | `int` | **字节数**，前端自行换算 `MB` |
| `metric` | `string` | 口径：`USS`（进程自测，同机首选）或 `RSS`（降级口径） |
| `threads` | `int \| null` | 线程数 |

响应片段示例：

```json
{
  "bot": {
    "started_time": 1735689600.0,
    "python_version": "3.12.0",
    "version": "v1.0.0",
    "web_render_status": true,
    "jobqueue_backend": "websocket",
    "command_parsed": 1234,
    "message_parsed": 5678
  },
  "processes": {
    "items": [
      {"name": "Server", "pid": 101, "memory": 314572800, "metric": "USS", "threads": 24},
      {"name": "daemon", "pid": 100, "memory": 52428800, "metric": "RSS", "threads": 3}
    ],
    "failures": [{"name": "jobqueue-hub", "reason": "timeout"}],
    "error": null
  }
}
```

- `metric` 在同一份响应中可能混用：`USS` 只能由进程自测（跨进程在 macOS 必然失败），
  守护进程与内置 Hub 由服务端按 PID 读取并降级为 `RSS`。前端应原样展示口径，不要混算。
- 采集结果在服务端缓存 5 秒，因此该接口不适合作为实时曲线数据源。
- 服务端离线时 `bot.jobqueue_backend`、`bot.command_parsed`、`bot.message_parsed` 为
  `null`，`processes` 退化为 `{"items": [], "failures": [], "error": "unavailable"}`，
  其余本机信息（CPU / 内存 / 磁盘）仍然返回。

## 5. 统计接口（`/api/analytics/modules`）

| 方法 | 路径 | 查询参数 |
| --- | --- | --- |
| `GET` | `/api/analytics/modules` | `days`（整数天，默认 `1`，最小 `1`）、`limit`（返回条数，默认 `50`，范围 `1`–`200`） |

按模块汇总时间窗口内的调用次数，按次数倒序、同次数按名称升序：

```json
{
  "days": 1,
  "count": 1234,
  "change_rate": 0.25,
  "total_modules": 12,
  "modules": [
    {"module_name": "wiki", "count": 300, "percent": 24.31},
    {"module_name": "ai", "count": 120, "percent": 9.72}
  ]
}
```

- `count` 为窗口内的总调用次数，`percent` 为该模块占窗口总数的百分比（保留两位小数）。
- `total_modules` 是窗口内出现过的模块数（**不受 `limit` 截断影响**），
  `modules` 只返回前 `limit` 条；前端据此提示「仅显示前 N 项」。
- `change_rate` 与既有 `/api/analytics` 同义：与上一个等长窗口相比的环比变化率（保留两位小数，
  上一窗口为空时为 `0.00`）。
- `module_name` 可能是空字符串（未归属模块的调用），前端应保留该行并本地化为「未知模块」。
- 数据来源于配置项 `enable_analytics` 开启时写入的统计记录：关闭该配置时接口仍可用，
  但只会返回空结果（`count: 0`、`modules: []`）。

## 6. 配置文件接口（`DELETE /api/config/{cfg_filename}`）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `DELETE` | `/api/config/{cfg_filename}` | 删除配置目录直属的 `*.toml` 文件 |

- 鉴权与既有配置接口一致；文件名必须是配置目录**直属**的 `.toml` 文件，
  传入子目录或 `..` 一律返回 `400 invalid_config_name`。
- **`config.toml` 受保护**：删除它会让下次启动以模板占位值重建，
  可能改掉 `db_path` 等不可重建的项，因此返回 `403 protected_config`。
- 文件不存在时同样返回成功（`changed: false`），便于前端幂等地重试。

响应（`200`）：

```json
{
  "changed": true,
  "restart_required": true,
  "cfg_files": ["config.toml", "jobqueue.toml", "module_wiki.toml"]
}
```

`cfg_files` 与 `GET /api/config` 同构（主配置置顶），前端可直接用它刷新文件列表。

### 6.1 生效时机（重要）

删除**只作用于磁盘文件**，接口不会改动运行中进程内存里的配置副本：

- 删除后，已在内存中加载的核心进程在**重启前仍沿用旧值**；
- 下次启动时，配置模板会按默认值重新生成该文件（即「删除」等价于「重启后恢复默认」）——
  这也意味着删除后无法通过接口把它「永久删掉」；
- 若在重启前有其它配置写入触发保存（例如交互式改配置），该文件会被按内存内容原样写回。

因此 `restart_required` 恒为 `true`，前端应提示用户「删除将在重启后生效（恢复默认值）」。

错误码：`400 invalid_config_name`、`403 protected_config`、`404 config_dir_not_found`、`401`。

## 7. WebRender 接口

浏览器归服务端进程所有，以下接口只做校验与转发；**未启用 WebRender** 时仍可查询状态，
但渲染类请求返回 `422 web_render_disabled`。

### 7.1 状态

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/webrender` | 本地配置回显 + 服务端实时状态 |

```json
{
  "config": {
    "enable": true,
    "browser_type": "chrome",
    "browser_executable_path": "",
    "headless": true,
    "remote_only": false,
    "remote_url": "http://127.0.0.1:8080/"
  },
  "status": {
    "available": true,
    "browser_initialized": true,
    "browser_mode": "headless",
    "headless": true,
    "keep_pages_open": false,
    "debug_mode": false,
    "remote_only": false,
    "remote_configured": false,
    "remote_timeout": 30,
    "export_logs": true,
    "logs_path": "./logs",
    "name": "AkariBot WebRender™",
    "contexts": [{"index": 0, "pages": ["https://example.test/"]}],
    "contexts_total": 1,
    "leaked": false
  }
}
```

- `config` 来自 WebUI 进程读到的配置，`status` 来自服务端；两者不一致时以 `status` 为准。
- `available` 表示「本地浏览器已就绪或已配置远端」；`leaked` 表示上下文数量与浏览器实际
  上下文数不一致（可用于排查页面泄漏）。`contexts` 用编号列表替代依赖库中以对象为键的字典。
- 服务端**无法读取渲染状态**（依赖库异常）时 `status` 为 `null`，前端应提示「运行状态不可用」；
  服务端进程整体离线时本接口返回 `400 Bad request`。

### 7.2 控制

| 方法 | 路径 | 请求体 | 限流 |
| --- | --- | --- | --- |
| `POST` | `/api/webrender/control` | `{"action": "start" \| "stop" \| "restart"}` | 6/分钟 |

效果与超级用户命令 `~webrender start` / `stop` / `reload` 一致；`start` 幂等
（浏览器已就绪时直接返回成功）。启停会创建或销毁浏览器进程，故限流较严。

```json
{"ok": true, "error": null, "status": {"available": true, "browser_initialized": true}}
```

- 只有请求本身不合法才返回 4xx（`400 invalid_json` / `invalid_body`、`422 invalid_action`）。
- 服务端离线、浏览器启动失败等运行期问题返回 **`200` 且 `ok: false`**，
  `error` 为 `init_failed` 或 `control_failed`，此时 `status` 为 `null`，
  前端应结合 `error` 展示失败原因。
- 控制成功后服务端会同步刷新 `Info.web_render_status`，
  因此 `GET /api/server-info` 的 `bot.web_render_status` 会随之变化。

### 7.3 渲染测试

| 方法 | 路径 | 限流 |
| --- | --- | --- |
| `POST` | `/api/webrender/test` | 10/分钟 |

请求体字段（未列出的字段一律忽略）：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `mode` | `string` | `status`（默认，只探测可用性）/ `source` / `screenshot` |
| `url` | `string` | 目标地址；`source` 模式必填 |
| `content` | `string` | 直接渲染的 HTML；与 `url` 二选一（`screenshot` 模式） |
| `element` | `string \| array` | 仅 `screenshot`：只截取该 CSS 选择器的元素；数组表示依次尝试 |
| `css` | `string` | 追加的自定义样式 |
| `width` / `height` | `int` | 视口尺寸 |
| `output_type` | `string` | `jpeg`（默认）/ `png` |
| `output_quality` | `int` | `jpeg` 质量，默认 `90` |
| `counttime` | `bool` | 是否在截图角落添加耗时标记，默认 `true` |
| `stealth` | `bool` | 反检测模式，默认 `true` |
| `wait_until` | `string` | `commit` / `domcontentloaded` / `load` / `networkidle`（默认） |
| `wait_after_load` | `int` | 加载后额外等待的毫秒数，`0`–`60000` |
| `locale` | `string` | 页面语言，默认 `zh_cn` |
| `raw_text` | `bool` | 仅 `source`：取 `<pre>` 内的纯文本 |

```json
{
  "ok": true,
  "mode": "screenshot",
  "elapsed": 1.82,
  "status": {"available": true, "browser_initialized": true},
  "source": null,
  "source_truncated": false,
  "images": ["<base64>"],
  "output_type": "jpeg",
  "error": null
}
```

- `images` 为**裸 base64**（与机器人内部消息格式一致），前端自行拼接 `data:image/jpeg;base64,`；
  单次最多返回 16 张，超出部分截断。
- `mode: "source"` 时 `source` 为页面源码（`raw_text: true` 时为纯文本），
  超过 200000 字符截断并置 `source_truncated: true`。
- `mode: "status"` 不渲染任何页面，`ok` 表示「浏览器已就绪或已配置远端」。
- 渲染失败属于运行期问题，返回 `200` 且 `ok: false`：

| 状态码 | `detail` / `error` | 含义 |
| --- | --- | --- |
| 400 | `invalid_json` / `invalid_body` | 请求体不是 JSON 对象 |
| 422 | `invalid_mode` | `mode` 不在三种取值内 |
| 422 | `missing_target` | `source` 缺 `url`，或 `screenshot` 缺 `url` 与 `content` |
| 422 | `web_render_disabled` | 配置未启用 WebRender |
| 200 | `browser_unavailable` | `mode: "status"` 下浏览器未就绪且未配置远端 |
| 200 | `render_failed` | 渲染过程失败（解析、超时、元素不存在等） |
| 200 | `invalid_options` | 参数超出依赖库限制（如 `wait_after_load` 越界） |

## 8. 前端接入建议

1. **首次加载**：`GET /api/policy/revision` 拿到三个资源的版本号，再按页面需要拉
   `/api/url-audit`、`/api/filter-words`；也可以直接拉后者，响应里同样带 `revision`。
2. **轮询同步**：定时（例如 30s）调用 `/api/policy/revision`，对比
   `url_audit.allowlist` / `url_audit.blocklist` / `filter_words`，
   仅当某个版本号变化时才重新拉取对应内容。
3. **条件请求**：拉取时带上 `?revision=<本地版本号>`，服务端未变化会返回 304，
   可直接复用本地缓存（注意 304 无响应体，axios 需允许 304 通过）。
4. **写入流程**：读取（带 `revision`）→ 本地编辑 → 提交时回传同一 `revision`。
   若返回 409，说明期间有命令或他人改动过，重新拉取后让用户确认再提交。
5. **全量同步**：URL 名单用 `PUT` 回传 `user_rules` 数组；词库用 `PUT` 回传
   `words` 数组。两者都是幂等的，适合做「编辑器保存」式交互。
6. **运行时生效**：词库写入后若返回 `runtime_synced: false`，
   提示用户需重启机器人才会生效（URL 名单无需重启）。
7. **后端 Mock**：在 akari-bot-webui 的 `src/mock/api.js` 中按上面的响应结构补充
   `/api/policy/revision`、`/api/url-audit`、`/api/filter-words` 等 mock，
   demo 模式下即可离线调试。
8. **状态页**：`GET /api/server-info` 限流为 6/分钟且进程占用在服务端缓存 5 秒，
   建议 30–60 秒轮询一次；`bot.command_parsed` / `bot.message_parsed` 是**自启动以来的累计值**，
   若要以「速率」展示，请自行按两次采样的差值除以间隔计算。
9. **统计页**：模块调用数量用 `GET /api/analytics/modules`，与既有 `/api/analytics`
   共用 `days` 语义，可一次请求同时取两份数据；`limit` 只影响返回条数，
   `total_modules` 始终是完整模块数。注意统计依赖配置项 `enable_analytics`。
10. **配置页**：删除接口返回 `restart_required: true`，请以「重启后恢复默认值」的措辞提示，
    并直接用响应里的 `cfg_files` 刷新列表；不要提供删除 `config.toml` 的入口（后端会 403）。
11. **WebRender 页**：先 `GET /api/webrender` 渲染状态卡片；`start` / `stop` / `restart`
    属重操作，按钮应加防重与等待态（接口限制 6/分钟）；「测试」按钮建议先发
    `{"mode": "status"}` 探测，再按用户输入发 `source` 或 `screenshot`。
    截图响应可能达数 MB，注意前端不要把它缓存进 localStorage。

## 9. 相关实现位置

| 位置 | 作用 |
| --- | --- |
| `bots/web/api/policy.py` | URL 审计名单与过滤词库接口实现 |
| `bots/web/api/api.py` | 状态（`/api/server-info`）、统计（`/api/analytics/modules`）与配置文件接口实现 |
| `bots/web/api/webrender.py` | WebRender 状态、控制与渲染测试接口实现 |
| `core/utils/url_audit.py` | 名单读写、规则校验与版本号相关的文件辅助方法 |
| `core/builtins/filter/__init__.py` | 词库加载、写入、即时重载 |
| `core/queue/contracts.py`、`core/queue/server.py` | `server.reload_filter_words`、状态统计、进程占用与 WebRender 控制等 RPC |
| `core/queue/diagnostics.py` | 各进程内存占用采集与序列化（`usage_payload`） |
| `core/database/models.py` | `AnalyticsData.get_modules_count_by_times`，模块调用次数的库内聚合 |
| `core/config/webrender.py`、`core/utils/web_render.py` | WebRender 配置与后端实例（仅在服务端持有浏览器） |
| `tests/unit/test_webui_policy.py`、`test_webui_status.py`、`test_webui_analytics.py`、`test_webui_config.py`、`test_webui_webrender.py` | 接口单元测试（临时目录 / RPC 替身，不触碰真实数据与浏览器） |
| `tests/unit/test_webrender_rpc.py`、`test_process_usage.py` | 服务端处理器与进程占用序列化的单元测试 |
